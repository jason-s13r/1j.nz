extends ../../src/layout/pug/_main.pug

block prepend title
  | Posts

block main
  - var posts = getPosts("/content/posts")
  - var paging = pagedList(posts, pageNum, pageSize)
  h1 Posts
  each post in paging.page
    include ../../src/layout/pug/_paged-post.pug
  div.pagination
    div.left.pagination-item
      if paging.hasPrev
        a(href='/content/posts/page/'+(pageNum-1)+'/index.pug') back
    div.right.pagination-item
      if paging.hasNext
        a(href='/content/posts/page/'+(pageNum+1)+'/index.pug') next